The repository containes files to implement null-interface algorithm in Vivado Design Suite.

To create a Vivado project run the command below in HDL directory:

	vivado -mode batch -source vivado_hdl_run.tcl

